WHITESPACE = _{ " " | "\t" | "\n" | "\r" }
NEWLINE = _{ "\n" | "\r\n" }

csv = { record ~ (NEWLINE ~ record)* ~ NEWLINE? }
record = { field ~ ("," ~ field)* }
field = { "\"" ~ (!"\"" ~ ANY)* ~ "\"" | (!("," | NEWLINE) ~ ANY)+ }
